import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AddCauseModal = () => {
  return (
    <View>
      <Text>AddCauseModal</Text>
    </View>
  );
};

export default AddCauseModal;

const styles = StyleSheet.create({});
